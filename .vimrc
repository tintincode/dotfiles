" --- Usability ---
set number          " Show line numbers
set ruler           " Show cursor position
set showmode        " Show mode (INSERT, VISUAL, etc.)
set wildmenu        " Better command-line completion

" --- Appearance ---
syntax on           " Enable syntax high lighting

" --- Indentation settings ---
set expandtab       " Insert spaces instead of tab characters
set shiftwidth=4    " Indent/outdent by 4 spaces
set softtabstop=4   " Press Tab/Backspace moves to nearest 4-space boundary
set tabstop=4       " Show a real tab character as 4 spaces wide


" --- Search ---
set hlsearch        " Highlight search matches
set incsearch       " Incremental search
